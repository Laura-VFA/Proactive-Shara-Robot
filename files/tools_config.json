[{
    "type": "function",
    "function": {
        "name": "record_face",
        "description": "Register a user in the system. It must be executed if and only if 'proactive_question' is 'who_are_you' in user input. Never otherwise.",
        "strict": true,
        "parameters": {
            "type": "object",
            "properties": {
                "username": {
                    "type": "string",
                    "description": "The name of the user that it's being introduced"
                }
            },
            "additionalProperties": false,
            "required": ["username"]
        }
    }
}]